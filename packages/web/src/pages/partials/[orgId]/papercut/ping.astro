---
import PingPapercut from "~/components/ping-papercut.astro";
import { POST } from "~/pages/api/papercut/ping";

export const partial = true;

const response = await POST(Astro);
const status = response.status;
---

<PingPapercut />
