---
type Props = {
  isInitial?: boolean;
};

const { isInitial = false } = Astro.props;
---

<div
  id=`${isInitial ? "initial-": ""}app-loading-indicator`
  class="flex h-screen w-screen items-center justify-center"
>
  <img src="/loading-indicator.svg" alt="Loading..." class="w-24" />
</div>
