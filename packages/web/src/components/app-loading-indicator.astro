---
import LoadingIndicator from "~/components/loading-indicator.astro";

import type { AppLoadingIndicatorProps } from "~/types/astro-component-props";

type Props = AppLoadingIndicatorProps;

const { isInitial = false } = Astro.props;
---

<div
  id=`${isInitial ? "initial-": ""}app-loading-indicator`
  class="flex h-screen w-screen items-center justify-center"
>
  <LoadingIndicator />
</div>
